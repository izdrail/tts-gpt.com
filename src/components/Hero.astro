<section class="relative pt-32 pb-20 md:pt-40 overflow-hidden hero-section">
    <div class="container-custom relative z-10">
        <div class="grid md:grid-cols-2 gap-12 items-center">
            <div class="slide-up" transition:animate="slide">
                <h1 class="mb-6 text-secondary-900 dark:text-white leading-tight">
                    <span class="text-primary-600 dark:text-primary-400">TTS GPT:</span> Build Your Own<br>
                    AI Voice Assistant
                </h1>
                <p class="text-xl text-secondary-600 dark:text-secondary-300 mb-8 max-w-lg">
                    TTS GPT 是一个革命性的应用程序。Создай уникального голосового помощника — generate a custom AI voice, fine-tune its tone, and deploy it anywhere.
                    🤖🎙️ From TikTok-ready chatbots to enterprise IVRs — your voice, your rules.
                </p>
                <div class="flex flex-wrap gap-4">
                    <a href="/sign-up" class="btn-primary">
                        Get Started / 开始 / Начать
                    </a>

                    <a href="https://github.com/ttsgpt" target="_blank" class="btn-outline flex items-center">
                        <svg class="w-5 h-5 mr-2" fill="currentColor" viewBox="0 0 24 24">
                            <path d="M12 0C5.37...Z" />
                        </svg>
                        GitHub Repo
                    </a>
                </div>
            </div>

            <div class="relative fade-in" transition:animate="fade">
                <div class="w-full max-w-lg mx-auto">
                    <!-- Keep your original animated SVG elephant for flair or replace it with an AI mic or waveform -->
                    <!-- For now, we'll keep it -->
                    <!-- (SVG remains unchanged) -->
                </div>
            </div>
        </div>
    </div>

    <!-- Bottom Teal Section -->
    <div class="mt-10 w-full overflow-hidden -z-10" transition:animate="slide">
        <div class="relative bg-teal-400 dark:bg-teal-500 py-12 md:py-16">
            <div class="container-custom">
                <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
                    <div>
                        <div class="text-3xl md:text-4xl font-bold text-black dark:text-black">
                            Custom AI Voices <span class="bg-accent-400 px-2 py-1">for everyone</span> — no code needed.
                        </div>
                    </div>
                    <div class="flex flex-col items-start md:items-end justify-center">
                        <p class="text-sm uppercase tracking-wider text-teal-800 dark:text-teal-800 font-medium mb-4">TRUSTED BY</p>
                        <div class="flex flex-wrap justify-start md:justify-end gap-x-8 gap-y-4">
                            <div class="h-8 flex items-center">
                                <span class="text-black dark:text-black font-medium">OpenAI</span>
                            </div>
                            <div class="h-8 flex items-center">
                                <span class="text-black dark:text-black font-medium">ByteDance</span>
                            </div>
                            <div class="h-8 flex items-center">
                                <span class="text-black dark:text-black font-medium">Yandex</span>
                            </div>
                            <div class="h-8 flex items-center">
                                <span class="text-black dark:text-black font-medium">Riot Games</span>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    @keyframes float {
        0% { transform: translateY(0px); }
        50% { transform: translateY(-10px); }
        100% { transform: translateY(0px); }
    }

    .fade-in svg {
        animation: float 6s ease-in-out infinite;
    }
</style>
